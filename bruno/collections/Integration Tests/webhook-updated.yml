info:
  name: webhook UPDATED
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/webhooks/appointments"
  headers:
    - name: X-Tenant-Id
      value: "{{tenantId}}"
    - name: X-Api-Key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
        "action": "UPDATED",
        "patient": {
          "externalId": "TEST-0001",
          "givenName": "Testpersoon Gewijzigd",
          "email": "updated@patientpingeling.nl",
          "phoneNumber": "+31600000002"
        },
        "appointment": {
          "externalId": "test-uuid-created-001",
          "scheduledAt": "2027-02-01T10:00:00+01:00",
          "service": "Cardiology",
          "location": "Kamer 2",
          "instructions": "Gewijzigde instructies"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
